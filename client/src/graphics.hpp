#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <string>
#include <cmath>
#include <GL/gl.h>
#include <GL/glu.h>

GLuint LoadTexture(const char* filename, int wrap);
float deg2rad(float val);

#endif
